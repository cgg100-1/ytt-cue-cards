# YTT Cue Cards

Mobile-first yoga teacher training cue cards built from the grouped session transcript.

The app distinguishes **Pose**, **Link**, and **Context** nodes and deliberately preserves the current transcript wording for validation.

To publish with GitHub Pages after making the repository public: **Settings → Pages → Deploy from a branch → main → / (root)**.
